<%= form_for @announcement,  :url => billboard_path(@announcement) do |f| %>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <%= f.submit "Update" %>
    <%= link_to 'Back to this announcement', billboard_path(@announcement) %>
    <%= link_to 'Back to index', billboard_index_path %>
<% end %>